#pragma once
#include <GameStorage.h>
#include <EventManager.h>
using namespace TAKO;
struct EventOptionSelectedSystem
{
    void Update(TAKO::GameStorage& gameStorage, EventManager& eventManager);
};
